<?xml version="1.0" encoding="UTF-8"?>
<root>
	<section>
		<sectioninfo>
			<keywordset>
				<keyword>loadScript</keyword>
			</keywordset>
		</sectioninfo>
		
	  
		<title>Description</title>
		<para>
			Inserts script links to the head element. Render the value of the component, after passing it to the <code>getResourceURL()</code> method of the ViewHandler for this application, and passing the result through the <code>encodeResourceURL()</code> method of the ExternalContext.		
		</para>
	</section>
</root>